# ⚡ МГНОВЕННЫЙ ЗАПУСК (30 секунд!)

## Demo Mode включен по умолчанию!

Никаких настроек не требуется. Просто запустите:

### 1️⃣ Backend (10 секунд)
```bash
cd backend

# Создайте venv
python -m venv venv

# Активируйте (выберите вашу ОС)
source venv/bin/activate          # Linux/Mac
venv\Scripts\activate             # Windows

# Установите минимальные зависимости (без faster-whisper!)
pip install fastapi uvicorn sqlalchemy python-multipart pydantic cryptography python-dotenv

# Запустите
python run.py
```

Вы увидите:
```
🎭 DEMO MODE ENABLED
============================================================
✓ No API keys required
✓ No model downloads needed
✓ Mock responses for all services
✓ Perfect for testing the UI
============================================================

INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 2️⃣ Frontend (20 секунд)
```bash
# Откройте новый терминал
cd frontend

# Установите зависимости
npm install

# Запустите
npm run dev
```

### 3️⃣ Откройте браузер
```
http://localhost:3000
```

## ✨ Готово!

Теперь вы можете:
- ✅ Загружать файлы
- ✅ Видеть демо транскрипции
- ✅ Редактировать тексты
- ✅ Чатить с AI (демо ответы)
- ✅ Тестировать весь UI

## 🎯 Что дальше?

1. **Изучите интерфейс** - все работает!
2. **Прочитайте [DEMO_MODE.md](DEMO_MODE.md)** - подробности о demo режиме
3. **Когда готовы к production** - читайте [QUICKSTART.md](QUICKSTART.md)

## 💡 Нужна реальная транскрибация?

Отредактируйте `backend/.env`:
```bash
DEMO_MODE=false
```

И установите все зависимости:
```bash
pip install -r requirements.txt
```

Подробнее: [DEMO_MODE.md](DEMO_MODE.md)
