# Руководство по контрибуции

Спасибо за интерес к улучшению WhisperTranscriber! Мы приветствуем любые контрибуции.

## Как контрибьютить

### Сообщения о багах

1. Проверьте, что баг еще не был сообщен в Issues
2. Создайте новый Issue с:
   - Четким описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемым и фактическим поведением
   - Скриншотами (если применимо)
   - Информацией об окружении (ОС, версия Python/Node, etc.)

### Предложения фич

1. Создайте Issue с тегом "feature request"
2. Опишите:
   - Что вы хотите добавить
   - Зачем это нужно
   - Как это должно работать
   - Примеры использования

### Pull Requests

1. Fork репозиторий
2. Создайте новую ветку:
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. Внесите изменения
4. Убедитесь, что код работает:
   - Backend: запустите сервер и протестируйте эндпоинты
   - Frontend: проверьте UI в браузере
5. Закоммитьте изменения:
   ```bash
   git commit -m "feat: add amazing feature"
   ```
6. Запушьте в ваш fork:
   ```bash
   git push origin feature/amazing-feature
   ```
7. Создайте Pull Request

## Стиль кода

### Python (Backend)

- Следуйте PEP 8
- Используйте type hints
- Документируйте функции docstrings
- Максимальная длина строки: 100 символов

Пример:
```python
def process_file(file_path: str, language: Optional[str] = None) -> Dict[str, Any]:
    """
    Process audio/video file for transcription.

    Args:
        file_path: Path to the file
        language: Optional language code

    Returns:
        Dictionary with processing results
    """
    # Implementation
    pass
```

### JavaScript/React (Frontend)

- Используйте ES6+ синтаксис
- Предпочитайте функциональные компоненты
- Используйте React Hooks
- Именуйте компоненты PascalCase
- Именуйте файлы компонентов PascalCase.jsx

Пример:
```jsx
export default function MyComponent({ prop1, prop2 }) {
  const [state, setState] = useState(null)

  useEffect(() => {
    // Side effects
  }, [])

  return (
    <div className="...">
      {/* JSX */}
    </div>
  )
}
```

## Структура коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая фича
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление build tasks, package manager configs и т.д.

Примеры:
```
feat: add YouTube download support
fix: correct timestamp formatting
docs: update README with new features
refactor: optimize transcription service
```

## Тестирование

### Backend

```bash
cd backend
pytest
```

### Frontend

```bash
cd frontend
npm test
```

## Локальная разработка

См. [QUICKSTART.md](QUICKSTART.md) для инструкций по запуску локально.

## Вопросы?

Если у вас есть вопросы о контрибуции:
1. Проверьте существующие Issues и Discussions
2. Создайте новый Discussion
3. Свяжитесь с мейнтейнерами

Спасибо за ваш вклад! 🙏
