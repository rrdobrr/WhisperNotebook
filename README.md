# WhisperTranscriber - Веб-сервис для транскрибации аудио и видео

Полнофункциональный веб-сервис для транскрибации аудио/видео с интеграцией AI-чата и мониторингом затрат.

## Основные возможности

### 📝 Транскрибация
- Загрузка аудио файлов (mp3, wav, flac, m4a, ogg, etc.)
- Загрузка видео файлов (mp4, mkv, mov, avi, webm, etc.)
- Загрузка по ссылкам YouTube
- Автоматическое извлечение и сжатие аудиодорожки
- Выбор метода транскрибации:
  - Локальная модель faster-whisper (медленнее, но приватно)
  - OpenAI Whisper API (быстрее, требует API ключ)
- Выбор языка транскрибации
- Опция добавления таймкодов
- Очередь обработки множественных файлов

### ✏️ Редактор текстов
- Markdown редактор с предпросмотром
- Автосохранение
- Функция поиска в тексте
- Копирование в один клик
- Автоматическая саммаризация через ChatGPT
- Кастомная обработка текста через промпт

### 💬 AI Чат
- Создание множественных переписок
- Стриминг ответов от LLM
- Выбор модели (GPT-4, GPT-3.5-turbo, GPT-4o, etc.)
- Прикрепление текстов к сообщениям через drag & drop
- Автоматическое форматирование вложений

### 📊 Мониторинг затрат
- Раздельный учет расходов на Whisper API и ChatGPT
- Расчет затрат на Railway
- Отображение баланса OpenAI API
- История транзакций
- Ежедневная статистика

### ⚙️ Настройки
- Управление OpenAI API ключом (зашифрованное хранение)
- Настройки по умолчанию для транскрибации
- Выбор темы (светлая/темная)
- Проверка валидности API ключа

## Технологический стек

### Backend
- FastAPI - современный веб-фреймворк
- SQLAlchemy - ORM для работы с БД
- SQLite - база данных
- faster-whisper - локальная транскрибация
- OpenAI API - облачная транскрибация и LLM
- yt-dlp - загрузка YouTube видео
- ffmpeg - обработка аудио/видео
- cryptography - шифрование API ключей

### Frontend
- React 18 - UI библиотека
- Vite - сборщик
- TailwindCSS - стилизация
- React Query - управление состоянием сервера
- Zustand - локальное состояние
- React Router - навигация
- React Markdown - отображение Markdown
- React Dropzone - загрузка файлов

## Установка и запуск

### Локальная разработка

#### Требования
- Python 3.12+
- Node.js 20+
- ffmpeg

#### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте ENCRYPTION_KEY

# Запуск
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend

```bash
cd frontend
npm install
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### Docker

```bash
# Создайте .env файл в корне проекта
echo "ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())')" > .env

# Запуск
docker-compose up -d
```

### Деплой на Railway

1. Создайте новый проект на [Railway](https://railway.app)
2. Подключите ваш GitHub репозиторий
3. Railway автоматически обнаружит `railway.toml` и `Dockerfile`
4. Добавьте переменные окружения:
   - `ENCRYPTION_KEY` - ключ шифрования (сгенерируйте новый)
   - `PORT` - Railway установит автоматически
5. Добавьте Volume для постоянного хранения:
   - Mount path: `/app/data`
   - Для SQLite базы данных
6. Деплой!

#### Переменные окружения для Railway

```
ENCRYPTION_KEY=<your-encryption-key>
DATABASE_URL=sqlite:////app/data/whispertranscriber.db
```

## Использование

### Первый запуск

1. Откройте приложение
2. Перейдите в **Настройки**
3. Добавьте ваш OpenAI API ключ (если планируете использовать API)
4. Настройте параметры по умолчанию

### Транскрибация

1. Перейдите на **Главную** страницу
2. Выберите метод транскрибации (локальный/API)
3. Выберите язык
4. Загрузите файлы или вставьте YouTube ссылку
5. Следите за прогрессом на странице **Транскрипции**
6. Откройте готовый текст в редакторе

### Работа с текстами

1. Откройте текст в редакторе
2. Редактируйте в Markdown формате
3. Используйте кнопку **Саммаризировать** для автоматической саммаризации
4. Или введите кастомный промпт для обработки текста
5. Сохраняйте изменения

### AI Чат

1. Создайте новый чат
2. Выберите модель в правом верхнем углу
3. Напишите сообщение
4. Для прикрепления текстов:
   - Переключите панель на "Тексты"
   - Перетащите нужные тексты в поле ввода
5. Отправьте сообщение

## API Документация

FastAPI автоматически генерирует интерактивную документацию:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## Мониторинг затрат

Приложение автоматически отслеживает:
- Расходы на Whisper API (по минутам аудио)
- Расходы на ChatGPT API (по токенам)
- Затраты можно экспортировать для анализа

Для учета затрат на Railway:
1. Перейдите в **Затраты**
2. Вручную добавьте расходы из дашборда Railway
3. Система будет отображать общую статистику

## Безопасность

- OpenAI API ключи хранятся зашифрованными в БД
- Используется Fernet шифрование (symmetric encryption)
- CORS настроен для безопасной работы
- Валидация загружаемых файлов (размер, тип)
- Автоматическая очистка временных файлов

## Ограничения

- Максимальный размер файла: 500MB
- Локальная транскрибация работает медленнее API
- Рекомендуется использовать GPU для локальной транскрибации (требует изменения конфигурации)

## Roadmap

- [ ] Пакетная обработка файлов
- [ ] Экспорт в различные форматы (DOCX, PDF, SRT, VTT)
- [ ] Улучшенная визуализация затрат (графики)
- [ ] Аутентификация пользователей
- [ ] API rate limiting
- [ ] Webhooks для уведомлений
- [ ] Интеграция с облачными хранилищами (S3, Dropbox)

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте Issues на GitHub
2. Создайте новый Issue с подробным описанием проблемы
3. Приложите логи и скриншоты

## Благодарности

- [faster-whisper](https://github.com/guillaumekln/faster-whisper) - быстрая локальная транскрибация
- [OpenAI](https://openai.com) - Whisper API и GPT модели
- [FastAPI](https://fastapi.tiangolo.com) - отличный веб-фреймворк
- [React](https://react.dev) - мощная UI библиотека
